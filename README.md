# 💳 Banking System

A **console-based Banking System** written in **C++** that allows users to manage client accounts. The system uses text files to store client data and provides functionalities for adding, updating, deleting, and searching clients.

---

## ✨ Features

- 📋 Display all clients in a tabular format.
- ➕ Add new clients with account number, PIN, name, phone, and balance.
- ❌ Delete clients safely after confirmation.
- ✏️ Update client information (PIN, name, phone, balance).
- 🔍 Search and display client information by account number.
- 🖥️ User-friendly menu interface.

---

## 📂 File Structure

- `main.cpp` – Main program file containing all the logic.
- `Clients.txt` – Text file storing all client records.
- `.gitignore` – Recommended to ignore `.vs/` and temporary files.

---

## 🚀 How to Use

1. Clone the repository:

```bash
git clone https://github.com/ilyasse578/Banking-System.git
